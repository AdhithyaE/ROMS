server:
  port: 8081
  error:
    include-stacktrace: never

spring:
  application:
    name: component-processing-service
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:componentsDb
    driver-class-name: org.h2.Driver

logging:
  level:
    org:
      springframework : debug
  file: 
   path: /
   name: log.txt
